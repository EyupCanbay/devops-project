# --- PROD PING SERVICE ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ping-prod
  namespace: production
  labels:
    app: ping
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ping
      env: prod
  template:
    metadata:
      labels:
        app: ping
        env: prod
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: env
                    operator: In
                    values:
                      - prod
              topologyKey: "kubernetes.io/hostname"
      containers:
        - name: ping
          image: eyupcanbay/ping-service:bb56cb669d7422d9ea1caff2b03dfafaa2c2312a
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: ping-port
            - name: PONG_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: pong-url
---
apiVersion: v1
kind: Service
metadata:
  name: ping-prod
  namespace: production
  # 👇👇👇 BURASI EKLENDİ! 👇👇👇
  labels:
    app: ping
  # 👆👆👆 SERVICE MONITOR BUNU ARAR 👆👆👆
spec:
  selector:
    app: ping
    env: prod
  type: NodePort
  ports:
    - name: http-port
      port: 8080
      targetPort: 8080

---
# --- PROD PONG SERVICE ---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pong-prod
  namespace: production
  labels:
    app: pong
    env: prod
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pong
      env: prod
  template:
    metadata:
      labels:
        app: pong
        env: prod
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchExpressions:
                  - key: env
                    operator: In
                    values:
                      - prod
              topologyKey: "kubernetes.io/hostname"
      containers:
        - name: pong
          image: eyupcanbay/pong-service:bb56cb669d7422d9ea1caff2b03dfafaa2c2312a
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: pong-port
            - name: PING_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: ping-url
          ports:
            - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: pong-prod
  namespace: production
  # 👇👇👇 BURASI EKLENDİ! 👇👇👇
  labels:
    app: pong
  # 👆👆👆 SERVICE MONITOR BUNU ARAR 👆👆👆
spec:
  selector:
    app: pong
    env: prod
  type: ClusterIP
  ports:
    - name: http-port
      port: 8081
      targetPort: 8081